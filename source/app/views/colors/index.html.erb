<h1> Feely </h1>

<h2> Choose a Color </h2>

<div class="colors-container">
  <% @colors.each do |color| %>
    <div class="color-selection" id="<%=color.name%>"><%= button_to color.name, new_color_stories_path(color), :id => color.name %></div>
  <%end%>
</div>
